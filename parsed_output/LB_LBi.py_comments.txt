46:         # program = _ expr _  →  discard surrounding whitespace nodes
51:         # expr = term (add_op term)*
52:         # Fold left for left-associativity of + and -.
62:         # term = factor (mul_op factor)*
63:         # Same fold pattern; * / bind tighter because they live under terms.
73:         # factor = number / paren  →  one-element list wrapping the winner
77:         # paren = "(" _ expr ")" _
82:         # Prefer node.text (full match) over visited_children for leaves.
83:         # Trailing whitespace from the grammar rule is fine; int() allows it.
87:         # Return the operator character for the fold in visit_expr.
94:         # Fallback for rules without visit_* (e.g. "_").
95:         # Return children if any, otherwise the node itself.
